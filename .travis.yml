language: swift # or objective-c
osx_image: xcode11
script:
  - xcodebuild -schemeOZChattingUI-Example -workspace Example/OZChattingUI.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6S,OS=10.0' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
